# 챗봇·NAI 경계 분리 기준선

기준일: 2026-07-28

## 코드 기준

- 저장소: `chikzon/nai-batch-generator`
- 커밋: `6fcbdccc9d16ccc6875e5e79e008235089b5e2a7`
- 작업 트리: clean
- 기준 회귀: `python 검증.py` — 147/147 통과
- 유료 NAI 호출: 하지 않음

## 변경 금지 경계

다음은 상위 작업 폴더의 별도 프로젝트다. NAI 정리·이동·모듈화의 대상이 아니다.

- `..\..\성향 표\` 전체
- `..\..\로그 편집기\` 전체

두 경로에는 파일 수정, 이동, 이름 변경, Git 조작, 테스트 산출물 생성을 하지 않는다.

## 사용자 데이터 경계

다음 경로는 코드 이동 대상에서 제외한다.

- `설정.json*`, `프로필/`, `캐릭터/`, `그림체/`, `세팅/`, `씬규격/`
- `조각/`, `수집/`, `태그/`, `output/`, 캐시, 생성 로그
- 옛 `이미지생성/`의 설정·상태·모델·수집·태그·output
- 챗봇 `에셋/`의 실제 이미지와 R2 백업

모듈화 중에는 파일명, JSON 구조, 저장 위치, 백업과 잠금 순서를 바꾸지 않는다.

## 기존 호출 경로

- 실행: `python start.py`, `실행.bat`
- 검증: `python 검증.py`
- 공개 수집 import: `import arca_public_import`
- 개인 자료: `--data-dir <경로>`
- 구형 자료: `asset_config.json`, `nsfw_seed_state.json` 호환 읽기

기존 호출 경로는 새 모듈의 호환 어댑터로 유지한다.

## 단계별 중단 기준

- 전체 회귀 실패
- 기존 import 또는 실행 명령 실패
- 사용자 JSON의 자동 변환·덮어쓰기
- 프롬프트·캐릭터·그림체·세팅 원문 변화
- 기능 이동과 기능 변경이 같은 커밋에 섞임

