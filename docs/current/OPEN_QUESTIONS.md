# 사용자 판단 대기

현재 공개 저장소 정리를 막는 질문은 없다.

- 내부 감사·과거 증거는 삭제하지 않고 저장소 밖에 보관한 뒤 Git 추적에서 제거한다.
- 현재 `main`만 일반 push로 정리하며 과거 Git 역사와 기존 태그·Release는 보존한다.
- 기능 코드·사용자 자료·테스트 계약은 정리 대상이 아니다.

과거 커밋의 파일까지 GitHub 역사에서 완전히 없애려면 별도의 비가역 작업이므로,
이번 정리가 끝난 뒤 사용자가 명시적으로 요청할 때만 검토한다.
