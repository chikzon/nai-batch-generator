# 현재 작업

상태: **완료 — 다음 작업 미지정**

## 완료한 작업

### 1. 옛 문서 상태 교정

- `../ai-review/01-기능증거색인.md`
- `../ai-review/통합작업실-계획및진행도-2026-07-28.md`

두 문서는 당시 커밋의 역사 기록이며 현재 권위 문서가 아님을 첫머리에 명시했다.
공개 저장소의 `AGENTS.md`와 `docs/current/INDEX.md`도 두 문서를 현재 상태 판정에
사용하지 않는다.

### 2. 모델·Quality·UC 순수 변환 경계 분리

다음 책임을 `src/nai_studio/domain/model_presets.py`로 옮겼다.

- 지원 모델과 UC 프리셋 목록
- 메타데이터 모델명→API 모델 ID 변환
- 모델별 Quality 태그의 합치기·분리·복원
- 모델별 UC 문구의 합치기·분리

`legacy_app.py`는 같은 이름을 명시적으로 import하므로 기존 호출과 테스트 호환을
유지한다. UI·저장 형식·payload·HTTP 호출·공식 문구는 변경하지 않았다.

## 검증 결과

- 이동 전 코드와 새 모듈의 상수·순수 변환 결과 바이트 동일
- 직접 관련 회귀 3/3
- 모듈 경계 검사 3/3
- Python 구문·import 통과
- 격리 데이터 앱 기동 HTTP 200
- 전체 회귀·새 테스트·빌드·Release는 실행하지 않음

다음 기능은 최신 사용자 지시로 새 `NEXT_TASK`를 정한 뒤 시작한다.
