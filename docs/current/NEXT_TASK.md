# 현재 작업

상태: **권위 문서의 옛 상태 표시 교정**

## 목적

저장소 밖에 보관된 다음 두 문서가 옛 커밋과 배포 상태를 현재 상태처럼 말하지 않게
한다.

- `../ai-review/01-기능증거색인.md`
- `../ai-review/통합작업실-계획및진행도-2026-07-28.md`

두 문서를 전면 재작성하지 않는다. 문서 첫머리에 당시 기준 커밋과 역사 문서임을
명확히 표시하고, 현재 상태 판정은 `docs/current/INDEX.md`, 실제 소스, 통과한 검사,
GitHub Release를 따르게 한다.

## 완료 조건

- 두 문서의 `76c7f62`, `846ded8`, 원격보다 70커밋 앞섬, 최신 빌드·Release 미완료
  표현이 **당시 기록**임을 명확히 표시한다.
- 현재 `main`과 `v1.1.0` Release 상태를 짧은 교정 주석으로 남긴다.
- 공개 저장소의 `AGENTS.md`와 `docs/current/INDEX.md`는 두 옛 문서를 현재 권위
  문서로 가리키지 않는다.
- 기능표 본문과 과거 시험 수치를 새 수치로 추측해 덮어쓰지 않는다.

## 검증

- 두 역사 문서의 첫머리와 현재 문서 우선순위를 눈으로 대조한다.
- 공개 저장소 Markdown에서 저장소 밖 `ai-review` 상대 링크가 0건인지 확인한다.
- 코드·테스트 변경이 0건인지 확인한다.
- 전체 회귀와 새 테스트는 실행하지 않는다.

## 금지 범위

- 기능 코드·테스트 수정
- 과거 수치를 현재 수치로 복사·추정
- 새 진행도·기능표 문서 추가
- 구조 분리 작업 동시 진행
- 사용자 자료·Git 역사·기존 Release 변경
- 저장소 밖의 성향 표와 로그 편집기 접근
