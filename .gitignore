# ── 개인 데이터 (절대 커밋하지 않는다) ─────────────────────────────
# 내 NAI 토큰·프롬프트·캐릭터가 들어 있다
설정.json
설정.json.bak
*.json.bak
설정.json.덮어쓰기전백업
*.덮어쓰기전백업
상태.json
nsfw_seed_state.json
generation_state.json
생성.log
*.log

# 프로필(계정별 데이터) 전체
프로필/

# 생성물
output/

# 내가 만든 씬 데이터 — 남에게 필수가 아니고 취향이 강하다
세팅/
씬규격/
asset_config.json

# 내 캐릭터·그림체·조각·씬·선별
캐릭터/*.json
!캐릭터/규격_설명.txt
그림체/
조각/
씬.json
선별.json

# ── 대용량 자료 ────────────────────────────────────────────────────
# 프로그램 본체와 자료의 생명주기를 나눈다. 자료는 자료팩으로 따로 받는다.
수집/이미지캐시/
수집/그림체.json
수집/작가통계.json
수집/레시피.json
수집/바이브/

# 단부루/e621 태그 사전 (6MB) · NAI 토크나이저 vocab (2.3MB) — 자료팩으로 받는다
태그/*.csv
t5_tokenizer.json
수집/태그색인.pickle
수집/태그색인.tmp

# ── 잡동사니 ──────────────────────────────────────────────────────
__pycache__/
*.pyc
*.zip
python/
.venv/
.DS_Store
Thumbs.db

# ── 기준선 커밋에서 추가 제외 (2026-07-27, 검토 라운드02) ──────────
# 설정.txt 는 설정.json 의 원본 대조본 — 토큰·개인 프롬프트·캐릭터가 들어간다
설정.txt
# 작가조합.json 은 공개 공유 프롬프트 조합 수집물 (그림체.json 과 같은 계열)
수집/작가조합.json

# 빌드 산출물 (빌드.py)
/build/
dist/
*.spec
