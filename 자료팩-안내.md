# 기본자료팩

이 ZIP은 NAI 배치 생성기 본체와 분리해서 넣는 공개 기본 자료다.

## 들어 있는 것

- 캐릭터·베이스 빌더 후보사전
- 규격과 기본 옵션
- 태그 검색·자동완성용 Danbooru/e621 태그 CSV

개인 캐릭터, 그림체, 레시피, 세팅, 생성물과 API 토큰은 들어 있지 않다. 세팅을 다른
사람에게 줄 때에는 앱의 세팅 내보내기를 명시적으로 사용한다.

## 넣는 방법

앱에서 `자료 → 자료 넣기`를 열고 이 ZIP을 그대로 끌어다 놓는다. 앱은 ZIP의
`manifest.json`과 파일별 SHA-256을 확인한 뒤 각 자료를 제 위치에 넣는다. 같은 팩을
다시 넣어도 사용자가 이미 가진 파일을 기본적으로 덮어쓰지 않는다.

태그 CSV와 T5 토크나이저의 출처·고정 버전·라이선스는
`THIRD_PARTY_NOTICES.md`를 따른다.
