# 필수 작업 규칙

작업 전 다음 문서를 읽는다.

1. `docs/current/INDEX.md`
2. `docs/current/NEXT_TASK.md`
3. `docs/current/OPEN_QUESTIONS.md`

`INDEX.md`가 가리키는 현재 기준 문서를 이전 채팅 기억과 추측보다 우선한다.
비교 앱의 코드는 정답 사양이 아니다. 원본 동작, 현재 NAI 규칙, 사용자 요구를 각각
대조한다.

`NEXT_TASK.md`에 적힌 한 작업만 수행한다. 관련 없는 기능, 재조사, 문서 추가,
대규모 리팩터링을 하지 않는다. 기능은 데이터 처리, 상태 유지, 예외 처리와 UI
연결까지 실제로 완성한다.

이 저장소 밖의 `성향 표`와 `로그 편집기`는 조사·수정·이동하지 않는다.

## 테스트 정책

- 변경과 직접 관련된 기존 테스트만 실행한다.
- 문서·증거·과거 기록 이동에는 코드 회귀를 실행하지 않는다.
- 파일 경로 변경에는 문법·import·architecture·앱 기동만 확인한다.
- 기능 변경에는 해당 기능의 기존 테스트만 실행한다.
- 새 테스트는 기존 테스트가 잡지 못한 실제 버그나 새로운 핵심 계약이 있을 때만
  추가한다.
- 새 테스트를 추가하기 전에 기존 테스트 확장으로 가능한지 확인한다.
- 전체 회귀는 릴리스 후보, 전역 저장 경계 변경, 또는 `NEXT_TASK.md`에 명시된
  경우에만 실행한다.
- 테스트 분리·통합·삭제를 기능 작업과 섞지 않는다.

요구사항 충돌, 데이터 손실 가능성, 기능 삭제가 필요하면 작업을 멈추고 사용자에게
질문한다.
