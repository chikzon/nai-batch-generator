# 필수 작업 규칙

작업 전 다음 문서를 읽는다.

1. `docs/current/INDEX.md`
2. `docs/current/NEXT_TASK.md`
3. `docs/current/OPEN_QUESTIONS.md`

`docs/current/INDEX.md`가 가리키는 현재 기준 문서를 이전 채팅 기억과 추측보다
우선한다. 기능의 현재 구현 상태는 **소스와 통과한 회귀**로 판정한다. 날짜나 커밋이
오래된 감사표·진행률·릴리스 인계는 역사 증거이며 현재 상태를 덮지 않는다.
비교 앱의 코드는 정답 사양이 아니다. 원본 동작, 현재 NAI 규칙, 사용자 요구를 각각
대조한다.

`NEXT_TASK.md`에 적힌 한 작업만 수행한다. 관련 없는 기능, 재조사, 문서 추가,
대규모 리팩터링을 하지 않는다. 기능은 데이터 처리, 상태 유지, 예외 처리와 UI
연결까지 실제로 완성한다.

이 저장소 밖의 `성향 표`와 `로그 편집기`는 조사·수정·이동하지 않는다.

## 구현 최소화 — Ponytail 상시 적용

코딩 작업은 문제와 실제 호출 경로를 먼저 확인한 뒤 아래 순서에서 처음 가능한 해법을
쓴다.

1. 추측성 필요면 만들지 않는다.
2. 저장소의 기존 헬퍼·타입·패턴을 재사용한다.
3. 표준 라이브러리, 플랫폼 기본 기능, 이미 설치된 의존성 순으로 사용한다.
4. 한 줄로 충분하면 한 줄로 끝낸다.
5. 마지막에만 돌아가는 최소 코드를 추가한다.

검증, 데이터 손실 방지, 보안, 접근성은 줄이지 않는다. 증상별 패치보다 모든 호출자가
지나는 기존 공통 경계를 한 번 고친다.

- `legacy_app.py`에 새 기능·라우트 분기를 추가하지 않는다. 새 기능은
  `src/nai_studio/services/`에서 구현하고 레거시에는 호환 연결만 둔다.
- 50줄을 넘는 함수는 그대로 늘리기 전에 책임 분리를 먼저 검토한다.
- 새 의존성·추상화·미래용 scaffolding은 실제 필요가 입증될 때만 추가한다.

## 휴리스틱 허용 및 검증 제한 — 절대 규칙

- 한 문제에 같은 관점의 해결·검증을 **최대 3회**까지만 시도한다.
- 3회 안에 해결되지 않으면 반복을 중단하고, 시도한 방법·현재 한계·다른 관점의
  인사이트·가장 나은 불완전한 우회안을 기록한 뒤 다른 작업을 계속한다.
- 항상 증상 패치보다 한 단계 위의 원인과 책임 경계를 먼저 본다. 다만 외부
  라이브러리·프레임워크 내부까지 파고들지 않고 즉시 적용 가능한 우회안을 우선한다.
- 핵심 사용자 흐름과 데이터 보존에는 엄격한 검증을 적용한다. 발생 가능성이 매우 낮은
  엣지 케이스와 부차적 최적화는 보편적인 80% 해법과 휴리스틱으로 처리한다.
- 정보가 부족해도 안전하고 되돌릴 수 있는 범위라면 가장 가능성 높은 가정을 명시하고
  끝까지 진행한다. 같은 내용을 반복 질문하거나 완벽한 논리를 기다리지 않는다.
- 조건이 충돌해 동시에 만족할 수 없으면 억지로 결론을 만들지 않는다. 검증을 멈추고
  선택지별 장단점과 현재 권고안을 기록한다.
- 데이터 손실, 기능 삭제, 비가역 변경, 사용자 범위를 벗어난 변경 가능성은 휴리스틱으로
  넘기지 않는다. 이 경우에만 작업을 멈추고 사용자 판단을 받는다.
- 완료 판단에 직접 필요하지 않은 조사·전수 검색·전체 회귀·문서 재작성은 하지 않는다.

## 주석 규칙

- 모든 줄을 설명하지 않는다. 공개 계약, 저장 경계, 비직관적 변환, 외부 연동,
  회귀 위험이 있는 핵심 코드에만 유지보수 가치가 있는 주석을 단다.
- 주석은 가능한 짧게 다음 네 가지를 드러낸다.
  1. 필요한 의존성
  2. 연결되는 모듈·함수·데이터
  3. 존재 이유
  4. 작동 방식과 결과
- 코드가 그대로 말해 주는 내용을 반복하거나, 실제 동작과 쉽게 어긋날 장문의 설명은
  남기지 않는다.

## 테스트 정책

- 변경과 직접 관련된 기존 테스트만 실행한다.
- 문서·증거·과거 기록 이동에는 코드 회귀를 실행하지 않는다.
- 파일 경로 변경에는 문법·import·architecture·앱 기동만 확인한다.
- 기능 변경에는 해당 기능의 기존 테스트만 실행한다.
- 새 테스트는 기존 테스트가 잡지 못한 실제 버그나 새로운 핵심 계약이 있을 때만
  추가한다.
- 새 테스트를 추가하기 전에 기존 테스트 확장으로 가능한지 확인한다.
- 전체 회귀는 릴리스 후보, 전역 저장 경계 변경, 또는 `NEXT_TASK.md`에 명시된
  경우에만 실행한다.
- 테스트 분리·통합·삭제를 기능 작업과 섞지 않는다.

요구사항 충돌, 데이터 손실 가능성, 기능 삭제가 필요하면 작업을 멈추고 사용자에게
질문한다.

## 상태 기록 규칙

- 기능을 구현하거나 검증한 커밋에서는 `docs/current/NEXT_TASK.md`를 함께 갱신한다.
- 전체 회귀 수치는 실행한 커밋과 함께 적는다. 옛 수치를 현재 수치처럼 복사하지 않는다.
- 포터블·설치본은 빌드 뒤 들어온 커밋이 하나라도 있으면 `최신` 또는 `완료`라고 쓰지
  않는다.
- `생성.log`의 오류 수는 테스트 로그가 분리되기 전까지 제품 장애 수로 사용하지 않는다.

## 출력·변경 제시 규칙

- 인사말·부연 설명·반복 요약 없이 핵심 결과물만 낸다.
- 가정, 못 한 것·미확인, 비가역 작업 전 확인은 짧게라도 반드시 적는다.
- 코드 수정 답변은 전체 파일을 다시 싣지 않고 diff 또는 바뀐 함수·블록만 제시한다.
